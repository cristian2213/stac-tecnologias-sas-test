const ROLES = {
  ADMIN: 'admin',
  CUSTOMER: 'customer',
};

const ROLES_VALUES = Object.values(ROLES);

const ROLES_KEYS = Object.keys(ROLES);

module.exports = {
  ROLES,
  ROLES_KEYS,
  ROLES_VALUES,
};
